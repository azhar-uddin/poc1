<h1>Reset Password</h1>

<%= form_for :user, url: send_password_reset_path do |f| %>
  <p>
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </p>
  <p class="button"><%= f.submit %></p>
<% end %>